---
phase: 03-foundation-resources
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - k8s/base/configmap-database.yaml
  - k8s/base/configmap-redis.yaml
  - k8s/base/configmap-application.yaml
  - k8s/base/secrets-README.md
  - k8s/base/kustomization.yaml
autonomous: true
requirements:
  - FOUND-04
  - FOUND-05
  - FOUND-06

user_setup:
  - service: kubernetes-secrets
    why: "Secrets must be created manually via kubectl after namespace exists"
    env_vars:
      - name: POSTGRES_PASSWORD
        source: "Generate with: openssl rand -base64 32"
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API Keys"
      - name: SUPABASE_ANON_TOKEN
        source: "Supabase Dashboard -> Settings -> API"
      - name: SUPABASE_SERVICE_TOKEN
        source: "Supabase Dashboard -> Settings -> API"
      - name: BULL_AUTH_KEY
        source: "Generate with: openssl rand -hex 16"
    dashboard_config:
      - task: "Create firecrawl-database-secret and firecrawl-api-secrets via kubectl"
        location: "Follow commands in k8s/base/secrets-README.md"

must_haves:
  truths:
    - "ConfigMaps exist for database, redis, and application configuration"
    - "ConfigMap values match Firecrawl .env.example non-sensitive settings"
    - "Secret creation runbook documents exact kubectl commands for database and API secrets"
    - "No actual secret values are committed to Git"
    - "kustomization.yaml includes all 3 ConfigMap files"
  artifacts:
    - path: "k8s/base/configmap-database.yaml"
      provides: "Postgres connection configuration"
      contains: "POSTGRES_HOST"
    - path: "k8s/base/configmap-redis.yaml"
      provides: "Redis connection configuration"
      contains: "REDIS_URL"
    - path: "k8s/base/configmap-application.yaml"
      provides: "Application runtime configuration"
      contains: "NUM_WORKERS_PER_QUEUE"
    - path: "k8s/base/secrets-README.md"
      provides: "Manual secret creation documentation"
      contains: "kubectl create secret"
    - path: "k8s/base/kustomization.yaml"
      provides: "Updated resource list with ConfigMaps"
      contains: "configmap-database.yaml"
  key_links:
    - from: "k8s/base/configmap-database.yaml"
      to: "Phase 5 postgres-service"
      via: "POSTGRES_HOST references K8s service DNS"
      pattern: "postgres-service\\.firecrawl\\.svc\\.cluster\\.local"
    - from: "k8s/base/configmap-redis.yaml"
      to: "Phase 5 redis-service"
      via: "REDIS_URL references K8s service DNS"
      pattern: "redis-service\\.firecrawl\\.svc\\.cluster\\.local"
    - from: "k8s/base/configmap-application.yaml"
      to: "Phase 6 Deployments"
      via: "envFrom configMapRef in Deployment manifests"
      pattern: "PLAYWRIGHT_MICROSERVICE_URL"
    - from: "k8s/base/secrets-README.md"
      to: "Manual kubectl workflow"
      via: "Human runs documented commands"
      pattern: "kubectl create secret generic"
---

<objective>
Create structured ConfigMaps for externalized application configuration and a secrets runbook documenting manual secret creation via kubectl.

Purpose: Separates configuration from container images (12-factor app). ConfigMaps are committed to Git for GitOps management. Secrets are documented but never committed, created manually by operator. Phase 6 Deployments will reference these ConfigMaps and Secrets via envFrom.

Output: 3 ConfigMap YAML files, secrets-README.md runbook, updated kustomization.yaml.
</objective>

<execution_context>
@/Users/gqadonis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gqadonis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-foundation-resources/03-RESEARCH.md

<interfaces>
<!-- From Plan 03-01: kustomization.yaml will already have namespace, serviceaccounts, rbac resources -->
From k8s/base/kustomization.yaml (after Plan 01):
```yaml
resources:
  - namespace.yaml
  - serviceaccounts.yaml
  - rbac.yaml
  - api-deployment.yaml
  - ui-deployment.yaml
images:
  - name: firecrawl-api
    newName: gcr.io/prometheus-461323/firecrawl-api
    newTag: "initial"
  - name: ingestion-ui
    newName: gcr.io/prometheus-461323/ingestion-ui
    newTag: "initial"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structured ConfigMaps for database, Redis, and application</name>
  <files>k8s/base/configmap-database.yaml, k8s/base/configmap-redis.yaml, k8s/base/configmap-application.yaml</files>
  <read_first>
    - apps/api/.env.example
    - .planning/phases/03-foundation-resources/03-RESEARCH.md
    - k8s/base/kustomization.yaml
  </read_first>
  <action>
**File 1 - k8s/base/configmap-database.yaml:**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: firecrawl-database
  namespace: firecrawl
  labels:
    app: firecrawl
    config-type: database
data:
  POSTGRES_HOST: "postgres-service.firecrawl.svc.cluster.local"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "firecrawl"
  USE_DB_AUTHENTICATION: "true"
```

**File 2 - k8s/base/configmap-redis.yaml:**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: firecrawl-redis
  namespace: firecrawl
  labels:
    app: firecrawl
    config-type: redis
data:
  REDIS_URL: "redis://redis-service.firecrawl.svc.cluster.local:6379"
  REDIS_RATE_LIMIT_URL: "redis://redis-service.firecrawl.svc.cluster.local:6379"
```

**File 3 - k8s/base/configmap-application.yaml:**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: firecrawl-application
  namespace: firecrawl
  labels:
    app: firecrawl
    config-type: application
data:
  HOST: "0.0.0.0"
  PORT: "3002"
  NUM_WORKERS_PER_QUEUE: "8"
  CRAWL_CONCURRENT_REQUESTS: "10"
  MAX_CONCURRENT_JOBS: "5"
  BROWSER_POOL_SIZE: "5"
  LOGGING_LEVEL: "INFO"
  PLAYWRIGHT_MICROSERVICE_URL: "http://playwright-service.firecrawl.svc.cluster.local:3000/scrape"
```

All values are non-sensitive configuration derived from apps/api/.env.example. Service hostnames use Kubernetes internal DNS format: `{service-name}.{namespace}.svc.cluster.local`. Sensitive values (passwords, API keys, tokens) are excluded -- those go in Secrets created via kubectl.
  </action>
  <verify>
    <automated>grep "POSTGRES_HOST" k8s/base/configmap-database.yaml && grep "REDIS_URL" k8s/base/configmap-redis.yaml && grep "NUM_WORKERS_PER_QUEUE" k8s/base/configmap-application.yaml && grep "kind: ConfigMap" k8s/base/configmap-database.yaml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - configmap-database.yaml contains `kind: ConfigMap` with name `firecrawl-database`
    - configmap-database.yaml contains POSTGRES_HOST=`postgres-service.firecrawl.svc.cluster.local`, POSTGRES_PORT=`5432`, POSTGRES_DB=`firecrawl`, USE_DB_AUTHENTICATION=`true`
    - configmap-database.yaml does NOT contain POSTGRES_PASSWORD or POSTGRES_USER (those are secrets)
    - configmap-redis.yaml contains `kind: ConfigMap` with name `firecrawl-redis`
    - configmap-redis.yaml contains REDIS_URL=`redis://redis-service.firecrawl.svc.cluster.local:6379`
    - configmap-redis.yaml contains REDIS_RATE_LIMIT_URL=`redis://redis-service.firecrawl.svc.cluster.local:6379`
    - configmap-application.yaml contains `kind: ConfigMap` with name `firecrawl-application`
    - configmap-application.yaml contains HOST=`0.0.0.0`, PORT=`3002`, NUM_WORKERS_PER_QUEUE=`8`
    - configmap-application.yaml contains PLAYWRIGHT_MICROSERVICE_URL with `playwright-service.firecrawl.svc.cluster.local`
    - No file contains any secret values (passwords, API keys, tokens)
    - All 3 files have namespace: firecrawl and labels app: firecrawl
  </acceptance_criteria>
  <done>3 ConfigMaps created with non-sensitive config. Database config points to postgres-service DNS. Redis config points to redis-service DNS. Application config matches .env.example defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Create secrets runbook and update kustomization.yaml</name>
  <files>k8s/base/secrets-README.md, k8s/base/kustomization.yaml</files>
  <read_first>
    - k8s/base/kustomization.yaml
    - apps/api/.env.example
    - .planning/phases/03-foundation-resources/03-RESEARCH.md
  </read_first>
  <action>
**File 1 - k8s/base/secrets-README.md:**

Create a markdown runbook documenting manual secret creation. This file IS committed to Git (it contains no actual secrets, only instructions and placeholder markers).

Content must include:

1. **Header:** "# Firecrawl Secrets - Manual Creation Guide" with note that secrets are NEVER committed to Git.

2. **Prerequisites:** firecrawl namespace must exist (`kubectl get namespace firecrawl`).

3. **Secret 1 - firecrawl-database-secret:**
```bash
kubectl create secret generic firecrawl-database-secret \
  --from-literal=POSTGRES_USER=firecrawl \
  --from-literal=POSTGRES_PASSWORD='<GENERATE: openssl rand -base64 32>' \
  --namespace=firecrawl \
  --dry-run=client -o yaml | kubectl apply -f -
```

4. **Secret 2 - firecrawl-api-secrets:**
```bash
kubectl create secret generic firecrawl-api-secrets \
  --from-literal=OPENAI_API_KEY='<FROM_OPENAI_DASHBOARD>' \
  --from-literal=SUPABASE_ANON_TOKEN='<FROM_SUPABASE_DASHBOARD>' \
  --from-literal=SUPABASE_SERVICE_TOKEN='<FROM_SUPABASE_DASHBOARD>' \
  --from-literal=SUPABASE_URL='<FROM_SUPABASE_DASHBOARD>' \
  --from-literal=BULL_AUTH_KEY='<GENERATE: openssl rand -hex 16>' \
  --namespace=firecrawl \
  --dry-run=client -o yaml | kubectl apply -f -
```

5. **Optional secrets section** listing keys that can be added if features are enabled:
   - SEARCHAPI_API_KEY
   - LLAMAPARSE_API_KEY
   - SCRAPING_BEE_API_KEY
   - SLACK_WEBHOOK_URL
   - STRIPE_SECRET_KEY
   - POSTHOG_API_KEY / POSTHOG_HOST
   - TEST_API_KEY

6. **Verification commands:**
```bash
kubectl get secrets -n firecrawl
kubectl describe secret firecrawl-database-secret -n firecrawl
kubectl describe secret firecrawl-api-secrets -n firecrawl
```

7. **Update/rotation instructions** showing how to update a single key and restart pods.

8. **Security notes:** Never commit secrets to Git, rotate every 90 days, consider sealed-secrets for v2.

**File 2 - Update k8s/base/kustomization.yaml:**

Add the 3 ConfigMap files to the resources list. Do NOT add secrets-README.md (it is documentation, not a K8s resource). The updated resources list should be:
```yaml
resources:
  - namespace.yaml
  - serviceaccounts.yaml
  - rbac.yaml
  - configmap-database.yaml
  - configmap-redis.yaml
  - configmap-application.yaml
  - api-deployment.yaml
  - ui-deployment.yaml
```

Keep the existing images section unchanged. Foundation resources (namespace, SA, RBAC, ConfigMaps) listed before deployment resources.
  </action>
  <verify>
    <automated>grep "kubectl create secret" k8s/base/secrets-README.md && grep "firecrawl-database-secret" k8s/base/secrets-README.md && grep "firecrawl-api-secrets" k8s/base/secrets-README.md && grep "configmap-database.yaml" k8s/base/kustomization.yaml && grep "configmap-redis.yaml" k8s/base/kustomization.yaml && grep "configmap-application.yaml" k8s/base/kustomization.yaml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - secrets-README.md contains `kubectl create secret generic firecrawl-database-secret`
    - secrets-README.md contains `kubectl create secret generic firecrawl-api-secrets`
    - secrets-README.md contains POSTGRES_USER and POSTGRES_PASSWORD keys
    - secrets-README.md contains OPENAI_API_KEY, SUPABASE_ANON_TOKEN, SUPABASE_SERVICE_TOKEN, BULL_AUTH_KEY keys
    - secrets-README.md contains verification commands with `kubectl get secrets -n firecrawl`
    - secrets-README.md does NOT contain any actual secret values (no real passwords, keys, or tokens)
    - secrets-README.md contains placeholder markers like `<GENERATE:` or `<FROM_`
    - kustomization.yaml resources list includes configmap-database.yaml, configmap-redis.yaml, configmap-application.yaml
    - kustomization.yaml still includes namespace.yaml, serviceaccounts.yaml, rbac.yaml, api-deployment.yaml, ui-deployment.yaml
    - kustomization.yaml still has images section with firecrawl-api and ingestion-ui entries
    - `kubectl kustomize k8s/base/` completes without errors
  </acceptance_criteria>
  <done>Secrets runbook documents exact kubectl commands for database and API secrets. kustomization.yaml includes all ConfigMaps. No secret values committed to Git.</done>
</task>

</tasks>

<verification>
- `kubectl kustomize k8s/base/` runs without errors and output includes ConfigMaps
- secrets-README.md contains exact kubectl commands but no actual credentials
- 3 ConfigMaps contain non-sensitive configuration only
- ConfigMap service hostnames use K8s DNS format (*.firecrawl.svc.cluster.local)
- kustomization.yaml lists all resources in correct order
</verification>

<success_criteria>
- 3 ConfigMap files exist with correct K8s DNS hostnames for services
- secrets-README.md documents firecrawl-database-secret and firecrawl-api-secrets creation
- No actual secret values exist in any committed file
- kustomization.yaml builds cleanly with all foundation + deployment resources
- ConfigMap keys match values from apps/api/.env.example
</success_criteria>

<output>
After completion, create `.planning/phases/03-foundation-resources/03-02-SUMMARY.md`
</output>
