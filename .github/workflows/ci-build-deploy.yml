name: CI Build and Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'apps/api/**'
      - 'apps/ui/ingestion-ui/**'
      - 'apps/playwright-service-ts/**'
      - '.github/workflows/ci-build-deploy.yml'
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  ACR_NAME: prometheusagsacr
  ACR: prometheusagsacr.azurecr.io

jobs:
  build-push-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    strategy:
      matrix:
        service:
          - name: firecrawl-api
            context: ./apps/api
            dockerfile: ./apps/api/Dockerfile
          - name: ingestion-ui
            context: ./apps/ui/ingestion-ui
            dockerfile: ./apps/ui/ingestion-ui/Dockerfile
          - name: playwright-service
            context: ./apps/playwright-service-ts
            dockerfile: ./apps/playwright-service-ts/Dockerfile

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract short SHA
        run: echo "SHORT_SHA=${GITHUB_SHA:0:7}" >> $GITHUB_ENV

      - name: Azure login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}


      - name: ACR login
        run: az acr login --name prometheusagsacr

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.dockerfile }}
          push: true
          tags: ${{ env.ACR }}/${{ matrix.service.name }}:${{ env.SHORT_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify image in ACR
        run: |
          image="${ACR}/${{ matrix.service.name }}:${SHORT_SHA}"
          for i in 1 2 3 4 5; do
            echo "Verification attempt $i of 5"
            if az acr repository show --name "$ACR_NAME" --image "${{ matrix.service.name }}:${SHORT_SHA}" >/dev/null 2>&1; then
              echo "Image verified: $image"
              exit 0
            fi
            echo "Image not yet available, waiting 10 seconds..."
            sleep 10
          done
          echo "Failed to verify image after 5 attempts"
          exit 1

      - name: Install kustomize
        run: |
          curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
          sudo mv kustomize /usr/local/bin/
          kustomize version

      - name: Update kustomization with new image
        run: |
          cd k8s/base
          kustomize edit set image \
            ${{ matrix.service.name }}=${ACR}/${{ matrix.service.name }}:${SHORT_SHA}
          echo "Updated kustomization.yaml:"
          cat kustomization.yaml

      - name: Commit and push manifest updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add k8s/base/kustomization.yaml

          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "ci: update ${{ matrix.service.name }} image to ${SHORT_SHA} [skip ci]"

          for i in 1 2 3; do
            if git push origin main; then
              echo "Successfully pushed manifest update"
              exit 0
            fi
            echo "Push failed (attempt $i/3), pulling and retrying..."
            git pull --rebase origin main
          done

          echo "Failed to push after 3 attempts"
          exit 1
